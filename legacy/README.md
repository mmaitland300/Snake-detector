# Legacy Scripts Archive

These scripts are preserved from the original Colab-era prototype.
They are archived for historical context only and are not part of the primary workflow.

The repo root no longer keeps compatibility copies of these script names; the package CLI is the maintained interface.

Use the package CLI instead:

- `snake-detector split`
- `snake-detector train`
- `snake-detector eval`
- `snake-detector predict`
